# Зависимости приложения (веб-интерфейс, приём документов, экспорт).
#
# Ядро (facts / corpus / retrieval / pipeline / verify / llm) работает на чистой
# стандартной библиотеке: если нужен только CLI-конвейер, этот файл можно не
# устанавливать вовсе. Всё перечисленное ниже нужно веб-приложению и слою приёма.
#
# Указаны минимальные версии. Для изолированного контура зафиксируйте точные:
#   pip freeze > requirements.lock.txt
# и переносите на сервер именно lock-файл вместе с колёсами (make wheels).
#
# Python: 3.11+ (см. pyproject.toml).

# --- веб-сервер -------------------------------------------------------------
fastapi>=0.110            # маршруты, валидация, OpenAPI на /api/docs
uvicorn[standard]>=0.27   # ASGI-сервер; [standard] даёт uvloop и httptools
python-multipart>=0.0.9   # разбор multipart/form-data — загрузка документов
itsdangerous>=2.1         # подпись cookie-сессий
jinja2>=3.1               # шаблоны страниц интерфейса

# --- документы --------------------------------------------------------------
python-docx>=1.1          # чтение DOCX библиотеки и экспорт отчёта в DOCX
pymupdf>=1.24             # извлечение текста и структуры из PDF (быстрое)

# --- вычисления -------------------------------------------------------------
numpy>=1.26               # векторные операции плотного поиска (эмбеддинги)

# --- форматы документов ------------------------------------------------------
# Базовый набор читает PDF, DOCX и текст. Презентации, Excel, старые форматы,
# DjVu и сканы — см. requirements-formats.txt и docs/17-formats.md.

# --- работа по https ---------------------------------------------------------
# Ничего ставить не нужно. Свой сертификат система выписывает средствами одной
# стандартной библиотеки (src/reportgen/web/certs.py): в изолированном контуре
# «поставьте пакет» — это не совет, а тупик. Раньше здесь значился пакет
# cryptography, и на машине без интернета установка https упиралась в него.
